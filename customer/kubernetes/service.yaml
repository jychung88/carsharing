apiVersion: v1
kind: Service
metadata:
  name: customer
  labels:
    app: customer
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8084
  type: LoadBalancer
  selector:
    app: customer